load("@rules_rust//rust:defs.bzl", "rust_test")
package(default_visibility = ["//visibility:public"])

rust_test(
	name = "versioned_buffer_test",
	srcs = ["test_versioned_buffer.rs"],
	deps = [
		"//client/common:versioned_buffer",
	],
)
